package com.example.voyagerimpl

class PrivateFunc {

    fun calculate(a: Int, b: Int): Int {
            return multiply(a, b)
        }

        private fun multiply(x: Int, y: Int): Int {
            return x * y
        }
    }

